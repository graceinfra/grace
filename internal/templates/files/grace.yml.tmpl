config:
  profile: {{ .Profile }}

datasets:
  jcl: {{ .HLQ }}.{{ .WorkflowName }}.JCL
  src: {{ .HLQ }}.{{ .WorkflowName }}.SRC
  loadlib: {{ .HLQ }}.LOAD

jobs:
  - name: {{ .JobName }}
    step: execute
    source: {{ .JobName }}.cbl
